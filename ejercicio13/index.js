'use strict';
 
/* preguntar cuantas vueltas da al sol*/ 
/* declara variable para el inicio en 0*/ 
// declarar variables conut


let vueltas =  Number(prompt("Ingrese el numero de vueltas")) ;
let contador  = 0 ;

while (contador < vueltas) { //esta es la condicion

    alert('vuelta completa'+contador); //contatenado

    //todas instrucciones+
    contador=contador+1; //contador++ operadores unearos esto es obligatorio
}
