'use strict';

//Crear una función que muestre en un alert todos los elementos
// unos (1)del siguiente array num 0,1,0,1,0

function showArray (nums){
    let i=0
    while(i<Array.length){
      if(nums)[i] ==1
      alert(array [i]);
      i++

    }
   
    
}
let array = [0,1,0,1]

showArray(array)