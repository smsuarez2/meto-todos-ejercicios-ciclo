"use strict";

let valorA = 2;
let valorB = 6;

alert("valorA > valorB: " + (valorA > valorB)); // mayor que 
alert( "valorA < valorB: " + (valorA < valorB)); // menor que
alert( "valorA >= valorB: " + (valorA >= valorB)); // mayor igual que
alert( "valorA <= valorB: " + (valorA <= valorB)); // menor igual que 
alert( "valorA == valorB: " + (valorA == valorB)); // igual
alert( "valorA != valorB: " + (valorA != valorB)); //diferencia

// Ingresar un numero y verificar si es positivo

alert( 0  > valorB);